name arrayStack;

fn () {
	print(("Started running xNVSE StackArr unit tests."));
    
    int i = 10;
    assert(i == 10);
    i = 20;
    assert(i == 20);

    for (int i = 0; i < 10; i++) {
        assert(i != 20);
    }

    assert(i == 20);

	print(("Finished running xNVSE StackArr unit tests."));
}